1.数据结构-线性表
1.1线性表（List）：零个或多个数据元素的有限序列
1.2特征：有序 有限
1.3线性表的顺序存储结构 优点(读取某个位置元素快  增加删除某一个元素慢)
    概念：是用一段地址连续的存储单元依次存储线性表的数据元素。  这里有个问题：java中的数组存放在什么位置？？？
1.4地址计算方法
    假设一个数据存储单元为c 则存放位置用LOC表示 则LOC(i+1)=LOC(i)+c   则LOC(i)=LOC(1)+(i-1)*c  则每个位置都可以算出
    则时间复杂度通过这个公式则为O(1);则把具有这个特点的存储结构的称为随机存取结构。
1.5分析下arraylist的自增
    源码为默认长度0 在加入的时候判断长度为0时 给个默认长度为10 然后每次为长度的一半加入。最后增大到Integer 的最大值。
    然后会判断加的数<0 后 会抛内存溢出的错误。 按位运算到最大值后加1为负数最小值。

1.6线性表的链式存储结构（链表）
    组成= 头指针(不为空 必要元素)
         +头节点(不存放数据 不是必要元素)
         +节点Node[数据域(存放数据元素的地方)+指针域(存放直接后继元素的域，称为指针或链)]
         +最后一个节点[数据域+Null(指针指向空)]